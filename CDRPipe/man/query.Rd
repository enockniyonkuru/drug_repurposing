% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing.R
\name{query}
\alias{query}
\title{Assemble per-experiment results with p-values and q-values}
\usage{
query(
  rand_cmap_scores,
  dz_cmap_scores,
  subset_comparison_id,
  analysis_id = "cmap",
  pvalue_method = "continuous",
  phipson_smyth_correction = TRUE
)
}
\arguments{
\item{rand_cmap_scores}{numeric vector of null scores from random_score()}

\item{dz_cmap_scores}{numeric vector of observed scores from query_score()}

\item{subset_comparison_id}{string label for this query (e.g., dataset name)}

\item{analysis_id}{string label for the analysis type (default: "cmap")}

\item{pvalue_method}{character; "discrete" or "continuous"
"discrete" = count-based: p = (# perms >= obs) / N_perms (transparent, discrete values)
"continuous" = with Phipson & Smyth correction: p = 1/(N+1) when count=0 (recommended)
Default: "continuous" (package standard, recommended for publication)}

\item{phipson_smyth_correction}{logical; apply Phipson & Smyth (2010) correction?
When TRUE and count=0, use p = 1/(N+1) instead of p=0
When FALSE, use raw p = count/N (allows p=0)
Only used when pvalue_method="continuous". Default: TRUE}
}
\value{
data.frame with experiment id, score, p, q, subset_comparison_id, analysis_id
}
\description{
Assemble per-experiment results with p-values and q-values
}
